# Sanity CMS connection — copy these into Vercel → Settings → Environment Variables.
# If these are left blank, the site simply uses its built-in content (nothing breaks).
# Get the project id from sanity.io/manage after creating your project (see SETUP-CMS.md).

NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
